{
  "name": "Proxy",
  "version": "0.4.0",
  "description": "Proxy node server to test multiple servers to bypass XSS (cross-site scripting) for testing in local",
  "scripts": {
    "start": "node proxy"
  },
  "dependencies": {
    "express": "^4.16.4",
    "http-proxy-middleware": "^0.19.1"
  }
}
